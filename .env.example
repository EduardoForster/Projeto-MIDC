POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=indicadores

# Optional runtime vars for frontends (used when running in Docker Compose)
# VITE_API_URL is used by the React app (Vite)
# API_URL is referenced by the Angular app when running in Compose
#VITE_API_URL=http://backend:8000
#API_URL=http://backend:8000
